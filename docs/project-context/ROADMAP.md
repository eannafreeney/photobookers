# Roadmap & next priorities

Edit this file to keep the auto-generated project summary aligned with team intent.
The build step regenerates `public/project-summary.txt` at deploy time (the
`/api/project-summary.txt` route also generates it live as a fallback).

- Start a digital magazine. Each monthly issue is based on a theme and selects 8-10 books from the database that have this theme and then present a piece about each one.
